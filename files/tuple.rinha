let c = 10;
let sumTen = fn (n) => {
  n + c
};

let getStr = fn (str) => {
  str + " with super powers"
};

let fib = fn (n) => {
  if (n < 2) {
    n
  } else {
    fib(n - 1) + fib(n - 2)
  }
};

print(((fib("bob"), getStr("my cat")), (sumTen(20), c + sumTen(fib(25)))))
